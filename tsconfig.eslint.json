{
  // extend your base config so you don't have to redefine your compilerOptions
  "extends": "./tsconfig.json",
  "include": ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx", "packages/*","example/"],
  "exclude": [
    // "scripts/**",
    "internals/templates/**",
    "server/**",
    "node_modules/**",
    "build/**",
    "coverage/**",
    "packages/**/node_modules",
    "**/node_modules"
  ]
}
